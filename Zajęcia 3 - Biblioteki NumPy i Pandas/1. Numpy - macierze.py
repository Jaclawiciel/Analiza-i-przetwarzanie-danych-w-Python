import numpy as np

a = np.array([[1, 2], [3, 4]])
b = np.array([[1, 2], [3, 4]])

add_result = np.add(a, b)
multiply_result = np.multiply(a, b)

print("Suma \n{}".format(add_result))
print("")
print("Iloczyn \n{}".format(multiply_result))
